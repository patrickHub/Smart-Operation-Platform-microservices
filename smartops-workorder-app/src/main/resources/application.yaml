spring:
  application:
    name: smartops-workorder-app
  datasource:
    url: jdbc:postgresql://localhost:5432/smartops
    username: smartops
    password: smartops
  flyway:
    enabled: true
    locations: classpath:db/migration
    schemas: workorder
    default-schema: workorder
    create-schemas: true
    table: flyway_schema_history

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        default_schema: workorder

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      client-id: smartops-workorder-app
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
smartops:
  services:
    asset:
      base-url: http://localhost:8082
  security:
    jwt:
      issuer: smartops-identity
      secret: ${SMARTOPS_JWT_SECRET:local-dev-secret-change-me-local-dev-secret-change-me}

  outbox:
    enabled: true
    polling-rate-ms: 5000
    max-attempts: 5

  kafka:
    topics:
      workorder-events: smartops.workorder.events
server:
  port: 8083
  
resilience4j:
  circuitbreaker:
    instances:
      assetService:
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        failureRateThreshold: 50
        waitDurationInOpenState: 10s
        permittedNumberOfCallsInHalfOpenState: 3
        ignoreExceptions:
          - ch.smart.operations.platform.shared.exceptions.DownstreamClientException

  timelimiter:
    instances:
      assetService:
        timeoutDuration: 2s

management:
  endpoints:
    web:
      exposure:
        include: health,info,circuitbreakers,circuitbreakerevents
  endpoint:
    health:
      probes:
        enabled: true

logging:
  level:
    root: INFO
    org.hibernate.SQL: warn



